<% if line_item == @current_item %>
  <tr class="line-item-highlight">
<% else %>
<tr>
<% end %>
  <td class="quantity"><%= line_item.quantity %></td>
  <td><%= line_item.product.title %></td>
<!--  <td class="price"><%#= number_to_currency(line_item.price) %></td>-->
  <td class="price"><%= number_to_currency(line_item.total_price) %></td>
  <td><%= button_to 'Delete product', line_item, method: :delete,
                    data: { confirm: 'Are you sure?' } %></td>
  <td><%= button_to 'Decrement quantity', line_item, method: :patch %></td>
<!--  <td><input class="button" type="button" value="Decrement quantity" onClick=-->
<!--    <%#= line_item.quantity == 1 ? destroy : line_item.decrement_quantity %>>-->
<!--  </td>-->
</tr>